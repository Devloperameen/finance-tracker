{
  "name": "finance-tracker",
  "version": "1.0.0",
  "scripts": {
    "build": "cd fintrack && npm install && npm run build",
    "start": "cd backend && node server.js",
    "install-all": "cd fintrack && npm install && cd ../backend && npm install"
  }
}
